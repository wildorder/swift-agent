# Swift Agent — Vision Document

> **Moved.** The canonical vision now lives at [`docs/vision.md`](./docs/vision.md).
>
> This file was previously a near-identical mirror of it. The two drifted (the
> mirror lagged on the Management API, endpoint alignment, and the Zod
> `inputSchema` contract), and `pipeline.config.json` already names
> `docs/vision.md` as `visionPath` — so `docs/vision.md` is the single source of
> truth and this pointer exists only so old links keep working.

**One-liner:** The realtime and tool-execution layer for AI agents. Self-host it
in one command; your tools run in your codebase, your data stays in your Postgres.

| Looking for | Go to |
| --- | --- |
| Positioning, personas, scope, roadmap | [`docs/vision.md`](./docs/vision.md) |
| What is actually built today | [`docs/as-built.md`](./docs/as-built.md) |
| End-to-end walk-through | [`docs/quickstart.md`](./docs/quickstart.md) |
| Repo overview and self-hosting ladder | [`README.md`](./README.md) |
